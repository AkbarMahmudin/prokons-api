const userValidator = require('./users');
const productValidator = require('./products');
const variantValidator = require('./variants');
const orderValidator = require('./orders');
const colorValidator = require('./colors');

module.exports = {
  userValidator,
  productValidator,
  variantValidator,
  orderValidator,
  colorValidator,
};
